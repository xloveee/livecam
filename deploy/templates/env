# Generated by deploy/install.sh for __DOMAIN__
# Operator email (certbot): __EMAIL__
# Copy shape matches deploy/.env.example — do not commit this file.

# Rust Core
SFU_PUBLIC_IP=__PUBLIC_IP__
SFU_INTERNAL_SECRET=__SFU_INTERNAL_SECRET__
SFU_UDP_PORT=50000
SFU_HTTP_HOST=127.0.0.1
SFU_HTTP_PORT=8080
SFU_BIND_IP=0.0.0.0
SFU_ARCHIVE_DIR=__INSTALL_ROOT__/archive
HLS_DIR=__INSTALL_ROOT__/hls

# Go Proxy
GO_LISTEN_PORT=8443
CLIENT_DIR=__INSTALL_ROOT__/client
RUST_CORE_URL=http://127.0.0.1:8080

STUN_URL=stun:stun.l.google.com:19302
# TURN_URL=turn:turn.example.com:3478
# TURN_USERNAME=
# TURN_CREDENTIAL=

# Session + broadcast page
SESSION_SECRET=__SESSION_SECRET__
BROADCAST_PASSWORD=__BROADCAST_PASSWORD__

# WHIP publish whitelist: comma-separated 32-char alphanumeric keys.
# Empty is fail-closed unless ALLOW_OPEN_PUBLISH=1 (L9).
ALLOWED_STREAM_KEYS=__ALLOWED_STREAM_KEYS__
PUBLIC_BASE_URL=https://__DOMAIN__

# Donations (SQLite)
DONATION_DB_PATH=__INSTALL_ROOT__/data/donations.db

# Offline banner image uploads
OFFLINE_BANNER_UPLOAD_DIR=__INSTALL_ROOT__/data/offline_banners
