# Rust build artifacts (1GB+)
rust-core/target/

# Go build artifacts
go-features/api/api-server

# OS junk
.DS_Store
Thumbs.db

# Editor state
*.swp
*.swo
*~
.idea/
.vscode/

# Environment files (may contain secrets)
.env
.env.*
!.env.example
!**/.env.example
*.env.local
deploy/.env

# Runtime data (SQLite, uploads — secrets, PII, stream keys in filenames)
data/

# VOD archive recordings (runtime; rust-core/archive/ also ignored below)
archive/

# Go build artifacts at repo root
/api-server

# TLS / signing / credentials (never commit)
*.pem
*.key
*.p12
*.jks
*.keystore
*.mobileprovision

# VOD archive recordings (runtime data, not source)
rust-core/archive/

# HLS segments (ephemeral, generated at runtime)
hls/


info/
build-plan-story.md
Build-plan.md
td.md

.vscode/
!rust-core/vendor/**/.vscode/
.cursorignore
ckpt/*
ckpt/
old/
old/*

neverdo.md
deploy/out/
*.deb
*.rpm
